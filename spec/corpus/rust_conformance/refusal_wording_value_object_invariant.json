{
  "domain": "examples/banking",
  "note": "InvariantViolation/value_object_invariant — the general VO-invariant message, promoted into RefusalWording::TEMPLATES/Vocabulary::RefusalTemplate as part of this same migration (it wasn't in the 37-entry table before). Verified genuinely broken before this fixture existed: unmodified Rust said 'PositiveMoney violates its invariant: a currency is a three-letter code' (no offered-fields dump at all), Ruby says 'PositiveMoney invariant violated — a currency is a three-letter code (given {\"cents\":100,\"currency\":\"US\"})'. Register a customer, open their account, then Credit it with a two-letter currency code — PositiveMoney's own `currency.to_s.size == 3` invariant fails. Deliberately NOT CustomerNumber/a `pattern:`-bearing field any more (feat/pattern-sweep-banking-pizzas added `pattern: '[^ \\t\\n\\r]'` to CustomerNumber.value, so a blank reference now fails TypeMismatch before this invariant ever runs) — PositiveMoney.currency was deliberately left pattern-free by that same sweep (it already has a real, different invariant), so it's still a genuine, unshadowed invariant-refusal example.",
  "steps": [
    {"verb": "Banking::Customer.Register", "args": {"reference": {"value": "cust-1"}, "name": {"given": "Ada", "family": "Lovelace"}, "email": {"address": "ada@example.com"}}},
    {"verb": "Banking::Account.Open", "args": {"customer_id": "cust-1", "number": {"value": "acct-1"}, "kind": {"name": "current"}, "daily_limit": {"cents": 100000}}},
    {"verb": "Banking::Account.Credit", "args": {"number": {"value": "acct-1"}, "amount": {"cents": 100, "currency": "US"}, "narrative": {"text": "deposit"}}}
  ]
}
