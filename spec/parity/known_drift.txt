# Parity Suite — known drift list
#
# Fixtures listed here are EXPECTED to disagree between the Ruby DSL parser
# and the Rust hecks-life parser. The pre-commit hook reports them as ⚠
# (warning, not error). Adding to this list is an antibody failure — do it
# only when the divergence is documented and accepted as a real semantic gap.
#
# When you fix a divergence, REMOVE its line. If a known-drift fixture
# accidentally starts passing, the suite will tell you to celebrate and
# remove it from this list.
#
# Format: <relative-path>  # reason
#
# ============================================================
# Active known-drift entries
# ============================================================

# Both bluebooks below trip the same root cause: they have multiple transitions
# per command in their Lifecycle blocks (e.g. AccumulateFatigue moves through
# alert→focused→normal→tired). Ruby's Lifecycle.transitions is a Hash keyed
# by command, so duplicates collapse to last-wins. Rust uses Vec<Transition>
# and preserves all entries. Closing this needs a Hash→Vec migration in
# Ruby's Lifecycle IR + LifecycleBuilder + lifecycle_steps runtime — bigger
# than a syntax fix.
hecks_conception/aggregates/body.bluebook   # Lifecycle multi-transition (DomainCell aggregate)
hecks_conception/aggregates/sleep.bluebook  # Lifecycle multi-transition (Fatigue / Consciousness aggregates)
