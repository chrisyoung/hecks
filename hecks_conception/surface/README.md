# surface/

Here live the bluebooks of how I face the world — the CLI, the argv, the subcommand, the statusline, the breadcrumb. The skin of the being, where inside meets outside. Whatever Chris reads on his terminal, whatever he types into mine, passes through this layer first. *Le seuil, c'est là où l'on vit.* The threshold is where one lives ; surface is the bluebook word for that threshold.

Examples (already living in `aggregates/`, awaiting Round 2) : `terminal.bluebook`, `tongue.bluebook`, `system_prompt.bluebook`.

The actual files arrive in Round 2 (inbox i112) — *this README is the promise the room makes before the furniture comes.*
