// GENERATED by bin/project_rust from examples/banking (uses_framework "Identity")'s canonical IR.
// Do not hand-edit — re-run bin/project_rust instead.
#![allow(dead_code, unused_variables)]
use crate::kernel::Expr;

#[derive(Debug, Clone, PartialEq)]
pub struct IdentityId {
    pub value: String,
}

impl crate::kernel::Fielded for IdentityId {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::Field;
        use crate::kernel::Value;
        match name {
            "value" => Some(Field::Value(Value::Str(self.value.clone()))),
            _ => None,
        }
    }
}


impl IdentityId {
    pub fn check_invariants(&self) -> Result<(), crate::kernel::Refusal> {
{
    let ctx = crate::kernel::EvalContext { args: &crate::kernel::NoFields, instance: self };
    if !crate::kernel::interpret(&Expr::Not(Box::new(Expr::Empty(Box::new(Expr::ToS(Box::new(Expr::Lookup("value"))))))), &ctx)?.truthy() {
        return Err(crate::kernel::Refusal::InvariantViolation("IdentityId violates its invariant: an identity id is present".to_string()));
    }
}
        Ok(())
    }
}

impl IdentityId {
    pub fn to_json(&self) -> crate::kernel::Json {
        crate::kernel::Json::Object(vec![
        ("value".to_string(), crate::kernel::Json::Str(self.value.clone())),
        ])
    }
}

impl IdentityId {
    pub fn from_json(v: &crate::kernel::Json) -> Result<Self, crate::kernel::Refusal> {
        Ok(Self {
        value: { let x = v.require("value", "IdentityId")?; x.as_str().map(|s| s.to_string()).ok_or_else(|| crate::kernel::Refusal::TypeMismatch("IdentityId.value: expected String".to_string()))? },
        })
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct Identity {
    pub identity_id: Option<IdentityId>,
}

impl crate::kernel::Fielded for Identity {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::{Field, Value};
        match name {
            "identity_id" => self.identity_id.as_ref().map(|v| Field::Nested(v)).or(Some(Field::Value(Value::Nil))),
            _ => None,
        }
    }
}

impl Identity {
    pub fn to_json(&self) -> crate::kernel::Json {
        crate::kernel::Json::Object(vec![
        ("identity_id".to_string(), self.identity_id.as_ref().map(|v| v.to_json()).unwrap_or(crate::kernel::Json::Null)),
        ])
    }
}

impl Identity {
    pub fn from_json(v: &crate::kernel::Json) -> Result<Self, crate::kernel::Refusal> {
        Ok(Self {
        identity_id: match v.get("identity_id") { Some(&crate::kernel::Json::Null) | None => None, Some(x) => Some(IdentityId::from_json(x)?), },
        })
    }
}

impl crate::kernel::ToJson for Identity {
    fn to_json(&self) -> crate::kernel::Json {
        Identity::to_json(self)
    }
}

impl Identity {
    pub fn extract_id(v: &crate::kernel::Json) -> Result<String, crate::kernel::Refusal> {
        let by_identity = (|| -> Option<String> {
            let c0 = v.dig("identity_id.value")?.to_id_component().ok()?;
            Some(c0)
        })();
        let by_id_key = v.get("id").and_then(|j| j.to_id_component().ok());
        let by_reference_key = v.get("identity").and_then(|j| j.to_id_component().ok());

        by_identity.or(by_id_key).or(by_reference_key).ok_or_else(|| {
            crate::kernel::Refusal::TypeMismatch("Identity: no identity found (tried identity_id.value, id, identity)".to_string())
        })
    }
}

impl crate::kernel::Fielded for RegisterArgs {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::Field;
        
        match name {
            "identity_id" => Some(Field::Nested(&self.identity_id)),
            _ => None,
        }
    }
}


#[derive(Debug, Clone)]
pub struct RegisterArgs {
    pub identity_id: IdentityId,
}

pub fn dispatch_register(
    repo: &mut impl crate::kernel::Repository<Identity>, args: RegisterArgs, mutations: &mut Vec<crate::kernel::MutationRecord>,
) -> crate::kernel::DispatchResult<Identity> {
        args.identity_id.check_invariants()?;

    crate::kernel::dispatch(
        repo,
        crate::kernel::Hydrate::Create {
        id: args.identity_id.value.to_string(),
        build: Box::new(|| Identity {
            identity_id: Some(args.identity_id.clone()),
        }),
    },
        "Register",
        "Identity::Identity",
        &args,
        &[

        ],
        None,
        |record| {
        let _ = record;
            Ok(())
        },
        &[

        ],
        &["IdentityRegistered"],
        args.to_json(),
        mutations,
    )
}

impl RegisterArgs {
    pub fn to_json(&self) -> crate::kernel::Json {
        crate::kernel::Json::Object(vec![
        ("identity_id".to_string(), self.identity_id.to_json()),
        ])
    }
}

impl RegisterArgs {
    pub fn from_json(v: &crate::kernel::Json) -> Result<Self, crate::kernel::Refusal> {
let unknown = v.unknown_keys(&["identity_id", "id"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "Register does not declare {} — it takes identity_id",
        unknown.join(", ")
    )));
}
        Ok(Self {
        identity_id: IdentityId::from_json(v.require("identity_id", "RegisterArgs")?)?,
        })
    }
}

