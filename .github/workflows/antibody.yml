name: Antibody

# Runs only on pull_request — push events have no meaningful base to
# diff against, so checking them invites silent false-greens.
on:
  pull_request:
    branches: ["**"]

jobs:
  antibody:
    name: No new non-bluebook code
    runs-on: ubuntu-latest
    steps:
      # Check out the PR's actual head commit, not the auto-generated
      # merge commit GitHub creates for pull_request events. Otherwise
      # HEAD's message is an auto 'Merge A into B' line with no
      # exemption markers, so real markers in the PR commit are
      # invisible to bin/antibody-check.
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.sha }}

      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.3"

      # actions/checkout@v4 with fetch-depth: 0 already gets full
      # history including every branch ref, so no extra fetch needed.

      - name: Run antibody (blocking)
        run: |
          set -e
          base="origin/${{ github.base_ref }}"

          # Emit GitHub annotations for each flagged file so they surface
          # directly on the PR as warnings next to the listing.
          if ! out=$(bin/antibody-check --base "$base" 2>&1); then
            status=1
          else
            status=0
          fi

          echo "$out"

          echo "$out" | awk '
            /^⚠ antibody:/ { flag=1; next }
            flag && /^    / { path=$1; sub(/^ +/, "", path); print "::warning file=" path "::Non-bluebook file touched — antibody flagged" }
            /^$/ { flag=0 }
          '

          exit $status
