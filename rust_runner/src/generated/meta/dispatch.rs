// GENERATED by bin/project_rust from the self-hosted language (lib/hecksagain/language/bluebook)'s canonical IR.
// Do not hand-edit — re-run bin/project_rust instead.
#![allow(dead_code, unused_variables)]
use crate::kernel::Expr;

#[derive(Debug, Clone, PartialEq)]
pub struct DispatchText {
    pub value: String,
}

impl crate::kernel::Fielded for DispatchText {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::Field;
        use crate::kernel::Value;
        match name {
            "value" => Some(Field::Value(Value::Str(self.value.clone()))),
            _ => None,
        }
    }
}


impl DispatchText {
    pub fn check_invariants(&self) -> Result<(), crate::kernel::Refusal> {

        Ok(())
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct Binding {
    pub key: String,
    pub value: String,
}

impl crate::kernel::Fielded for Binding {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::Field;
        use crate::kernel::Value;
        match name {
            "key" => Some(Field::Value(Value::Str(self.key.clone()))),
            "value" => Some(Field::Value(Value::Str(self.value.clone()))),
            _ => None,
        }
    }
}


impl Binding {
    pub fn check_invariants(&self) -> Result<(), crate::kernel::Refusal> {

        Ok(())
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct Position {
    pub value: i64,
}

impl crate::kernel::Fielded for Position {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::Field;
        use crate::kernel::Value;
        match name {
            "value" => Some(Field::Value(Value::Int(self.value))),
            _ => None,
        }
    }
}


impl Position {
    pub fn check_invariants(&self) -> Result<(), crate::kernel::Refusal> {

        Ok(())
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct Dispatch {
    pub handler_id: Option<String>,
    pub handler: Option<DispatchText>,
    pub command_name: Option<DispatchText>,
    pub with_spec: Vec<Binding>,
    pub position: Option<Position>,
}

impl crate::kernel::Fielded for Dispatch {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::{Field, Value};
        match name {
            "handler_id" => self.handler_id.as_ref().map(|v| Field::Value(Value::Str(v.clone()))).or(Some(Field::Value(Value::Nil))),
            "handler" => self.handler.as_ref().map(|v| Field::Nested(v)).or(Some(Field::Value(Value::Nil))),
            "command_name" => self.command_name.as_ref().map(|v| Field::Nested(v)).or(Some(Field::Value(Value::Nil))),
            "with_spec" => Some(Field::Value(Value::List(self.with_spec.len()))),
            "position" => self.position.as_ref().map(|v| Field::Nested(v)).or(Some(Field::Value(Value::Nil))),
            _ => None,
        }
    }
}

impl crate::kernel::Fielded for DeclareArgs {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::Field;
        use crate::kernel::Value;
        match name {
            "handler_id" => Some(Field::Value(Value::Str(self.handler_id.clone()))),
            "handler" => Some(Field::Nested(&self.handler)),
            "command_name" => Some(Field::Nested(&self.command_name)),
            "position" => Some(Field::Nested(&self.position)),
            _ => None,
        }
    }
}


#[derive(Debug, Clone)]
pub struct DeclareArgs {
    pub handler_id: String,
    pub handler: DispatchText,
    pub command_name: DispatchText,
    pub position: Position,
}

pub fn dispatch_declare(
    repo: &mut impl crate::kernel::Repository<Dispatch>, args: DeclareArgs,
) -> crate::kernel::DispatchResult<Dispatch> {
        args.handler.check_invariants()?;
        args.command_name.check_invariants()?;
        args.position.check_invariants()?;

    let mut payload = std::collections::BTreeMap::new();
    payload.insert("handler_id".to_string(), format!("{:?}", args.handler_id)); 
        payload.insert("handler".to_string(), format!("{:?}", args.handler.value)); 
        payload.insert("command_name".to_string(), format!("{:?}", args.command_name.value)); 
        payload.insert("position".to_string(), format!("{:?}", args.position.value)); 

    crate::kernel::dispatch(
        repo,
        crate::kernel::Hydrate::Create {
        id: format!("{}:{}", args.handler_id.to_string(), args.command_name.value.to_string()),
        build: Box::new(|| Dispatch {
            handler_id: Some(args.handler_id.clone()),
            handler: Some(args.handler.clone()),
            command_name: Some(args.command_name.clone()),
            with_spec: vec![],
            position: Some(args.position.clone()),
        }),
    },
        "Declare",
        "Bluebook::Dispatch",
        &args,
        &[

        ],
        None,
        |record| {
        let _ = record;
            Ok(())
        },
        &["SendDeclared"],
        payload,
    )
}

impl crate::kernel::Fielded for BindArgs {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::Field;
        
        match name {
            "key" => Some(Field::Nested(&self.key)),
            "value" => Some(Field::Nested(&self.value)),
            _ => None,
        }
    }
}


#[derive(Debug, Clone)]
pub struct BindArgs {
    pub key: DispatchText,
    pub value: DispatchText,
}

pub fn dispatch_bind(
    repo: &mut impl crate::kernel::Repository<Dispatch>, id: &str, args: BindArgs,
) -> crate::kernel::DispatchResult<Dispatch> {
        args.key.check_invariants()?;
        args.value.check_invariants()?;

    let mut payload = std::collections::BTreeMap::new();
    payload.insert("key".to_string(), format!("{:?}", args.key.value)); 
        payload.insert("value".to_string(), format!("{:?}", args.value.value)); 

    crate::kernel::dispatch(
        repo,
        crate::kernel::Hydrate::Act { id: id.to_string() },
        "Bind",
        "Bluebook::Dispatch",
        &args,
        &[

        ],
        None,
        |record| {
        record.with_spec.push(Binding { key: args.key.value.clone(), value: args.value.value.clone() });
            Ok(())
        },
        &["BindingAttached"],
        payload,
    )
}

