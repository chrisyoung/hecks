# Billing Domain Glossary

## Invoice

An Invoice has a pizza_id (String).
An Invoice has a quantity (Integer).
An Invoice has a status (String).
You can create an Invoice with pizza id and quantity. When this happens, an Invoice is created. (command)
You can look up Invoices by pending. (query)
When an Order is placed, the system will create Invoice. (policy)

