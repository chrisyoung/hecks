Hecks.hecksagon do
  # Cross-domain event subscriptions
  # Identity listens to ModelRegistry and Operations events for audit logging
  subscribe "ModelRegistry"
  subscribe "Operations"

  # ModelRegistry listens to Compliance and RiskAssessment events
  subscribe "Compliance"
  subscribe "RiskAssessment"
end
